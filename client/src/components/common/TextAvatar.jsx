import React from "react";
import { Avatar } from "@mui/material";

const TextAvatar = ({ text }) => {
  const stringColor = (str) => {
    let hash = 0;
    let i;
  };
  return <div>TextAvatar</div>;
};

export default TextAvatar;
